pyshark>=0.6.0
google-generativeai>=0.3.0
scapy>=2.5.0
nest_asyncio>=1.5.8
# Note: Wireshark (includes tshark) is a required system dependency.
# Install on macOS: brew install wireshark
# Install on Ubuntu/Debian: sudo apt-get install wireshark
# These cannot be installed via pip and are not Python packages.
# Note: Wireshark/tshark and tcpdump are required system dependencies (see README)